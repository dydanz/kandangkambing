---
name: state-management
description: NOT APPLICABLE — NanoClaw has no frontend. State is managed via SQLite (SharedMemory) and JSON (TaskStore).
---

NanoClaw has no frontend. State is managed via `SharedMemory` (SQLite) and `TaskStore` (JSON). This agent is not applicable.
